type: google.api.Service
config_version: 3

http:
  rules:
    # lit-llm.proto
    - selector: litrpc.LLM.AnalyzeNode
      post: "/v1/llm/analyze"
      body: "*"
      response_body: "*"
      additional_bindings:
        - post: "/v1/llm/analyze/stream"  # Add streaming endpoint
